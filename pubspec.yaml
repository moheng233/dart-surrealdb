name: surrealdb
description: SurrealDB SDK for Dart - Connect to remote and embedded SurrealDB instances
version: 0.1.0
repository: https://github.com/moheng233/dart-surrealdb

environment:
  sdk: ^3.0.0

dependencies:
  web_socket_channel: ^3.0.0
  http: ^1.2.0
  uuid: ^4.5.1
  meta: ^1.15.0
  cbor: ^6.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
